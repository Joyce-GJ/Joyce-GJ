### ```git init``` 初始化文件夹为git文件夹
### ```git add .``` 将你的修改加入缓存
### ```git commit -m"hjl是zhu"``` 将缓存区的数据保存本地
### ```git push origin main``` 将本地修改提交到云端
### ```git pull origin main``` 将云端修改拉到本地
# 坑
1. 文件要先保存再add .
2. 线上修改后 本地要先pull再push 不然就混乱
3. 遇到报错不要急 先开加速器后开梯子 如果还是不行就问ai
